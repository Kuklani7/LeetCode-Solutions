class Solution {
    public int largestAltitude(int[] gain) {
        int max_alt=0;
        int curr=0;
        for(int i=0;i<gain.length;i++){
            curr=curr+gain[i];
            if(curr>max_alt){
                max_alt=curr;
            }
        }
        return max_alt;
    }
}
